<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <item
        android:id="@+id/menu_events"
        android:icon="@drawable/event_menu_icon"
        android:title="Show Events"
        app:showAsAction="ifRoom"
        android:visible="true"

        />
    <item
        android:id="@+id/menu_orders"
        android:icon="@drawable/order_menu_icon"
        android:title="Show Orders"
        app:showAsAction="ifRoom"
        android:visible="true"/>

    <item
        android:id="@+id/menu_user"
        android:icon="@drawable/user_menu_icon"
        android:title="View Profile"
        app:showAsAction="ifRoom"
        android:visible="true"
        />
</menu>